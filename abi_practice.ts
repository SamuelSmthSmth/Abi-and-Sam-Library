const multiplicator = (a, b, printText) => {
  console.log(printText,  a * b);
}

multiplicator(2, 4, 'Multiplied numbers 2 and 4, the result is:');

//When you get the chance, install deno to run your TypeScript files
//Use the command 'irm https://deno.land/install.ps1 | iex' in the terminal and restart vscode
//Then, to run a file, simply type deno with your file name (e.g. deno abi_practice.ts)